/// @description Use this enum for message types when creating a new network message
///				 Just add a new enum value and you're good to go

// The two default values are just samples, you can delete
// without hurting anything
enum NetMessageType {
	connect,
	disconnect
}